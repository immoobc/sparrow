[project]
name = "sparrow"
version = "0.1.0"
description = "Sparrow — A股量化数据基座"
requires-python = ">=3.11"

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
